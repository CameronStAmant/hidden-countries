import './MenuBar.css';

const menuBar = () => {
  return (
    <div className="menu">
      <div className="navItems">
        <div>Find That State!</div>
        <div>Timer</div>
      </div>
    </div>
  );
};

export default menuBar;
